#ifndef EXAMPLE_H
#define EXAMPLE_H
#include <stdio.h>
#define NUMBER 10
#define OUT(text, value) printf(text, value)
#define MATH(firstValue, secondValue) (firstValue + 10 + secondValue)*(2-1)
void itsMyPrint();

#endif // EXAMPLE_H
